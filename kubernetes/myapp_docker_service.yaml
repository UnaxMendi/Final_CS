apiVersion: v1
kind: Service
metadata:
  name: myinsuranceapp-service
  labels:
    app: myinsuranceapp
spec:
  selector:
    app: myinsuranceapp
  type: NodePort
  ports:
  - port: 5000
    targetPort: http
    nodePort: 32163 #30000-32767
    protocol: TCP
    name: http